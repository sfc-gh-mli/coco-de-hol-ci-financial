# Only needed if you are REGENERATING the dataset. The generated CSVs and PDF are already
# committed under data/, so normal facilitator setup does not require any of this.
#
#   python3 -m pip install -r facilitator/generator/requirements.txt
#
# Lower bounds are what the scripts were developed and verified against. Newer versions are
# fine --- the generator is seeded, and its --verify mode will tell you immediately if a
# library change altered the output.
numpy>=1.20
pandas>=1.3
fpdf>=1.7
